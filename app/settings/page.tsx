"use client";
import { useState } from "react"; import { Sidebar } from "../components";
export default function Settings(){const [ai,setAi]=useState(true);const [local,setLocal]=useState(true);return <div className="app-shell"><Sidebar active="settings"/><main className="main"><header className="topbar"><div><h1>设置</h1><p>让记录方式更贴近你的工作节奏</p></div></header><div className="content list-page"><div className="section-heading"><div><div className="eyebrow">PREFERENCES</div><h2>偏好设置</h2><p>简单、克制，只保留真正有用的选择。</p></div></div><div className="settings-grid"><Setting title="个人信息" hint="用于日记中的岗位标记" value="PQE"/><Setting title="AI 整理" hint="使用 AI 将关键词整理为完整记录" control={<button className={`switch ${ai?"on":""}`} aria-label="切换 AI 整理" onClick={()=>setAi(!ai)}/>} /><Setting title="本地保存" hint="在当前浏览器保留日记副本" control={<button className={`switch ${local?"on":""}`} aria-label="切换本地保存" onClick={()=>setLocal(!local)}/>} /><Setting title="外观" hint="当前主题" value="安静浅色"/><Setting title="关于企业实践日记" hint="把每天的实践，慢慢变成自己的成长记录" value="v1.0"/></div></div></main></div>}
function Setting({title,hint,value,control}:{title:string;hint:string;value?:string;control?:React.ReactNode}){return <div className="setting-row"><div><span>{title}</span><small>{hint}</small></div><div className="setting-control">{control||value}</div></div>}
