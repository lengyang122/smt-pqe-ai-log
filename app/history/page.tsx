"use client";
import { useEffect,useMemo,useState } from "react"; import Link from "next/link"; import { Sidebar } from "../components";
type Log={id:string;date:string;input:string;content:string;reflection:string};
export default function History(){const [logs,setLogs]=useState<Log[]>([]);const [q,setQ]=useState("");useEffect(()=>{try{setLogs(JSON.parse(localStorage.getItem("smt-pqe-logs")||"[]"))}catch{}},[]);const filtered=useMemo(()=>logs.filter(x=>(x.date+x.input+x.content+x.reflection).toLowerCase().includes(q.toLowerCase())),[logs,q]);return <div className="app-shell"><Sidebar active="history"/><main className="main"><header className="topbar"><div><h1>历史记录</h1><p>回看每一个认真工作的日子</p></div></header><div className="content list-page"><div className="section-heading"><div><div className="eyebrow">YOUR JOURNAL</div><h2>时间线</h2><p>{logs.length?`已经留下 ${logs.length} 篇记录`:"记录会安静地出现在这里"}</p></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索记录…"/></div><div className="timeline">{filtered.length?filtered.map((log,i)=><div className="timeline-item" key={log.id}><div className="timeline-date"><strong>{log.date.match(/\d+/)?.[2]||"—"}</strong>{log.date}</div><div className="timeline-main"><h3>{i===0?"最近的一天":"工作记录"}</h3><p>{log.content||log.input}</p><small>{log.reflection}</small></div><div className="item-actions"><Link className="secondary-btn" href="/">查看</Link><button className="secondary-btn" onClick={()=>{const next=logs.filter(x=>x.id!==log.id);setLogs(next);localStorage.setItem("smt-pqe-logs",JSON.stringify(next))}}>删除</button></div></div>):<div className="empty-state">还没有记录。去写下今天的第一段实践吧。<br/><Link href="/" style={{color:"var(--accent)",display:"inline-block",marginTop:12}}>新建日记 →</Link></div>}</div></div></main></div>}
